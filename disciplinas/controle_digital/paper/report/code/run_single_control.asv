load_single_sys;
load_single_lqg;

set_param('sim_single_complete','AlgebraicLoopSolver','LineSearch');
simOut = sim('sim_single_complete');

hfig = figure('units','normalized','outerposition',[0 0 1 1]);
sol.x = tOut;
y = get(simOut, 'x');
sol.y = y.signals.values;

plot_single_states(hfig, sol);
